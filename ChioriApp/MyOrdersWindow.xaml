<Window x:Class="ChioriApp.MyOrdersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Мои заказы" Height="400" Width="700">
    <Grid Margin="10">
        <DataGrid x:Name="dgOrders" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding OrderId}" Width="60"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding OrderDate, StringFormat=d}" Width="100"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat=F2}" Width="80"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding StatusId}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>